# ══════════════════════════════════════════════════════════════════════════════
# UNFAT 선택적 의존성 — Python 3.14에서 소스 빌드 또는 대안 필요
#
# 이 파일의 패키지들은 install.sh가 자동으로 처리합니다.
# 수동 설치:  pip install -r requirements-optional.txt
#
# 설치 실패해도 핵심 기능(AI 분석, 논문 처리)은 정상 동작합니다.
# 해당 기능만 비활성화됩니다.
# ══════════════════════════════════════════════════════════════════════════════

# ── GUI 자동화 (마우스·키보드 제어) ─────────────────────────────────────────
# Python 3.14: 바이너리 wheel 없음 → 소스 빌드 시도
# Linux: apt install python3-xlib xdotool 필요
# macOS: brew install python-tk 필요
# 설치 실패 시: 손 제스처 컴퓨터 제어, 브라우저 자동화 기능 비활성화
pyautogui

# ── 창 관리 ──────────────────────────────────────────────────────────────────
# Python 3.14: 바이너리 wheel 없음 → 소스 빌드 시도
# 설치 실패 시: 창 전환 기능 비활성화 (나머지 기능 정상)
pygetwindow

# ── 화자 인증 (목소리 등록) ──────────────────────────────────────────────────
# resemblyzer는 webrtcvad(C 익스텐션)에 의존합니다.
# Python 3.14에서 webrtcvad 바이너리 wheel 미제공.
#
# install.sh 처리 순서:
#   1) webrtcvad 소스 빌드 시도 (gcc/clang 필요)
#   2) 실패 시 webrtcvad 스텁 모드로 resemblyzer만 설치
#      → 화자 인증은 동작하나 무음 구간 필터링(VAD) 정확도 약간 저하
resemblyzer
