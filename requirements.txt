# ══════════════════════════════════════════════════════════════════════════════
# UNFAT 코어 의존성 — Python 3.11+ 호환, Python 3.14 완전 지원
#
# 설치 방법:
#   빠른 설치:  bash install.sh
#   수동 설치:  pip install -r requirements.txt
#              pip install -r requirements-optional.txt  (선택적 기능)
# ══════════════════════════════════════════════════════════════════════════════

# ── 음성 인식 ─────────────────────────────────────────────────────────────────
sounddevice
SpeechRecognition

# 한국어 음성 인식 (faster-whisper — 로컬 오프라인, 고정밀)
# Google Speech API보다 한국어 정확도 대폭 향상, 인터넷 불필요
# 첫 실행 시 모델 다운로드 (~39MB for tiny, ~244MB for small)
faster-whisper

# ── Google AI ─────────────────────────────────────────────────────────────────
google-genai
google-generativeai

# ── 이미지·네트워크 기본 ──────────────────────────────────────────────────────
pillow
requests
beautifulsoup4
duckduckgo-search
playwright

# ── 클립보드·화면 캡처 ───────────────────────────────────────────────────────
pyperclip
mss

# ── 컴퓨터 비전 ──────────────────────────────────────────────────────────────
opencv-python
mediapipe
numpy

# ── 시스템 유틸리티 ───────────────────────────────────────────────────────────
psutil
send2trash

# ── 미디어·오피스 ─────────────────────────────────────────────────────────────
youtube-transcript-api
python-pptx

# ── 웹 서버 ──────────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
cryptography
python-multipart

# ── QR 코드 ──────────────────────────────────────────────────────────────────
qrcode[pil]

# ── Windows 전용 ──────────────────────────────────────────────────────────────
comtypes; sys_platform == "win32"
pycaw; sys_platform == "win32"
win10toast; sys_platform == "win32"
pywinauto; sys_platform == "win32"

# ── 멀티 AI 연동 ─────────────────────────────────────────────────────────────
# Claude AI (Anthropic) — 논문 심층 분석, 각주, 연구사 정리
anthropic

# ── 역사학 연구 특화 의존성 ──────────────────────────────────────────────────
# PDF 페이지별 텍스트 추출 (각주 페이지 번호 지원)
pdfplumber

# 워드 파일 텍스트 추출
python-docx

# 한국어·영어·한자 OCR (스캔 이미지, 스캔 PDF, 한자 병용 문헌)
easyocr

# 딥러닝 프레임워크 (easyocr, resemblyzer 공통 사용)
# Python 3.14: cp314 wheel 제공됨 (torch 2.7+)
torch
torchvision

# ── 선택적 패키지 안내 ────────────────────────────────────────────────────────
# 아래 패키지들은 Python 3.14 바이너리 wheel 미제공으로 별도 설치가 필요합니다:
#   bash install.sh        → 자동 처리 (권장)
#   또는 requirements-optional.txt 참조
